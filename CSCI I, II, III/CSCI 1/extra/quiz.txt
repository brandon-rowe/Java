
Dialog box
J Option pain
key clicks, mouse events

Panel:
	class Panel extends JPanel
	Create a panel and set attributes.
	Add image.
	Add button.
	
Panel2:
	class Panel2 extends JPanel
	create buttons and demonstrate actionlistener
	add label, panel, button, listener

Panel3:
	class Panel3 extends JPanel
	creates paint method using Graphics page
	

Panel4:
	class Panel4 extends JPanel
	using timer and images
	moves image based on time
	create reboundListener with action performed method	

Panel5:
	class Panel5 extends JPanel
	mouse listener
	class dotslistener using mouselistener
	mouse pressed event.
	mouseClicked, mouseReleased, mouseEntered, mouseExited events

Panel6:
	class Panel6 extends JPanel
	addKeyListener(DirectionListener())
	setFocusable
	private class DirectionListener implements KeyListener {
            //--------------------------------------------------------------
            //  Responds to the user pressing arrow keys by adjusting the
            //  image and image location accordingly.
            //--------------------------------------------------------------
            public void keyPressed(KeyEvent event) {
                switch (event.getKeyCode()) {
                    case KeyEvent.VK_UP:
                        currentImage = up;
                        y -= JUMP;
                        break;

                    case KeyEvent.VK_DOWN:
                        currentImage = down;
                        y += JUMP;
                        break;
                    case KeyEvent.VK_LEFT:
                        currentImage = left;
                        x -= JUMP;
                        break;
                    case KeyEvent.VK_RIGHT:
                        currentImage = right;
                        x += JUMP;
                        break;
                }
	


Panel7:
	JOptionPane.showInputDialog










